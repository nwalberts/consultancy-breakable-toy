@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    @apply font-mono font-normal;
    font-size: 18px;
    line-height: 24px;
  }

  h1 {
    @apply text-4xl;
  }

  h3 {
    @apply text-xl;
  }
}
